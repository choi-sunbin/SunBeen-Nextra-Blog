이번 시간에는 WebHook을 활용해서 코드가 푸시됐을 때 자동으로 배포가 시작되도록

만들어 보겠습니다.

그리고 지난 시간에 이어서 스크립트도 살짝 수정을 해야 하는데요.

이 부분도 실습을 해보면서 문제를 식별하고 수정해 보겠습니다.

자, 지금까지 우리가 진행했던 걸 한번 되돌아보겠습니다.

제가 앞서 설명드렸던 이 그림대로 배포가 진행되고 있는 겁니다.

우리는 Jenkins에 접속을 해서 배포 버튼을 눌러서 배포를 하고 있습니다.

그럼 Jenkins에서는 배포 명령을 받아 파이프라인 스크립트에 적힌 대로 GitHub에서

소스 코드를 다운로드 받고 빌드와 패키징 과정을 거칩니다.

그리고 최종적으로 패키징된 애플리케이션 실행 파일을 SCP를 통해 서버에 전송해서

실행시키고 있는 거죠.

그런데 이렇게 매번 버튼을 눌러서 배포하고 싶지 않다면 어떻게 해야 할까요?

예를 들어, 소스코드가 GitHub에 Commit, Push 되기만 해도 스크립트를

실행시키고 싶다면 어떻게 할 수 있을까요?

바로 그걸 가능하게 해주는 게 WebHook이라는 겁니다.

WebHook을 통해 배포가 진행되도록 하면 이런 구조가 됩니다.

첫 번째로 배포 명령을 버튼을 눌러서 하는 게 아니라 GitHub 레포지토리에 코드를

Commit하고 Push합니다.

그럼 이거 자체가 배포가 시작되는 명령으로 이어지는데요.

GitHub에서 WebHook이라는 걸 설정해서 push가 일어나면 그걸 미리 지정한

곳으로 API 요청이 날아가도록 할 수 있습니다.

여기서 이 WebHook이라는 표현 자체는 어떤 일이 벌어지면 그 일 뒤에 따라오는 동작을

설정하는 걸 의미합니다.

보통은 콜백이라고 많이 표현을 하죠.

아무튼 푸시가 됐다는 이벤트가 전달이 되면 Jenkins는 앞서 이야기했던 배포 과정을

시작합니다.

GitHub에서 소스 코드를 다운로드 받고 빌드, 패키징 과정을 거쳐서 서버에 패키징된

파일을 전송하고 실행하는 거죠.

설명은 여기까지 하고 그럼 실습을 하러 가보겠습니다.

먼저 실습에 사용할 GitHub 레포지토리가 필요한데요.

지금까지 여러분들이 여러분들 각자의 GitHub 레포지토리를 사용했다면 그대로 진행을 하면

되겠지만 저랑 같이 제가 이 실습에서 제시한 deploy-test라는 제 GitHub

레포지토리에 있는 걸 그대로 사용을 하셨으면 지금 여기서부터는 실습을 하기가 조금

어렵습니다.

왜냐하면 여기서부터는 이제 개인적인 설정들이 들어가기 때문입니다.

그래서 가급적 여기서 어떻게 진행을 하는 게 좋냐면 여기서 포크를 해주시길 바랍니다.

포크를 해서 여러분들의 GitHub 레포지토리 내에서 진행을 해주세요. 그래서 이 디플로이

테스트라는 걸 포크를 하면 새로운 GitHub 레포지토리를 이걸 그대로 따와서 만들 수가

있겠죠.

자 그래서 포크하는 방법에 대해서 설명을 드리면 여기서 이 포크를 눌러서 여기에

지금 저 같은 경우에 제가 현재 가지고 있는 이 LLE-LLE0이라는 제 GitHub

계정에는 이미 이 레포지토리가 있기 때문에 포크를 할 수 없습니다.

근데 아마 여러분들은 포크를 할 수 있을 거구요.

레포지토리 이름은 그대로 사용하셔도 되고 마음대로 하셔도 됩니다.

아니면 여러분들이 앞에서 다른 레포지토리를 쓰고 있었으면 그걸 그대로 쓰셔도 되고요.

그래서 결과적으로 얘를 포크를 해서 그 포크된 레포지토리에서 진행을 해주세요.

아무튼 여러분들이 여러분들의 권한을 가진 GitHub 레포지토리를 만들었다고 가정을 하고

다음으로 넘어가 보겠습니다.

먼저 토큰을 만들어 줘야 되는데요.

토큰을 만들어 주기 위해서는 여러분들의 GitHub 계정을 누르신 다음에 여기서

Settings로 들어가시면 됩니다.

그리고 아래쪽으로 내려서 Developer Settings라고 있어요.

Developer Settings에서 Personal Access Tokens에 위에 보면

Fine-grained Tokens라는 게 있는데 아직은 베타라서 이걸 사용하진 않겠습니다.

이게 제가 알고 있는 거대로면 GitHub 레포지토리별로 좀 권한을 관리할 수 있는 그런

토큰으로 알고 있는데 좀 더 안전한 방법을 사용할 수 있는 거죠.

이 밑에 있는 이 토큰스의 클래식이라고 되어 있는 건 아마 레포지토리별로 권한 관리는 할

수 없을 겁니다.

어쨌든 지금 이게 베타 딱지가 붙어 있어서 아직 이걸로 강의를 진행하기는 어려울 것 같고

나중에 여러분들이 이게 안정화된 버전이 나오면 이걸 사용하셔도 괜찮겠지만 지금 실습에서는

이 토큰의 클래식을 사용하겠습니다.

자 그러면 여기서 이 Generate new token이라는 버튼이 있어요.

이걸 눌러서 여기서 Generate new token(classic)을 눌러줍니다.

저는 이렇게 OTP를 설정해 놨기 때문에 이렇게 Authentication 코드를 입력하는 창이 나오는데

여러분들은 설정 안 해놨으면 안 나올 수도 있습니다.

근데 어쨌든 여기서 이걸 입력을 하고 다음으로 넘어갑니다.

자, 이제 토큰을 만들어줘야 하는데, 자, 지금 만드는 토큰의 사실 목적은 어떤 거냐면

Jenkins가 이 GitHub 레포지토리에 API 호출을 통해서 여러 가지 액션을 할 수

있는 권한을 주는 거예요.

그래서 권한을 어떤 걸 줄 수 있나 보면은 여기 보시면 레포지토리, 근데 정확히는 프라이빗

레포지토리에 대한 권한을 주는 것들이 있고 그리고 퍼블릭 레포지토리들에 대해서도 추가적인

권한을 주는 것도 있습니다.

초대하는 거에 대한 것도 있고 여러 가지 이걸 API 호출을 통해서 할 수 있는 거고

워크플로우를 토큰을 가지고 API로 제어를 할 수 있는 것도 있고 그 외에 여러 가지

기능들이 있습니다.

물론 여기서 이 WebHook을 사용하는데 필요한 것은 모두가 필요한 건 아니에요.

일단은 여기다가 Jenkins 토큰이라고 이름을 짓겠습니다.

그리고 여기에 이 토큰의 만료 기간을 지정할 수가 있는데 여기서 지금 7일,

그리고 30일,

그리고 60일, 90일도 있고 그리고 커스텀으로 날짜를 직접 지정할 수도 있고 아니면

만료가 없게도 사용을 할 수 있습니다.

뭐 Jenkins라고 하면은 그냥 만료가 없게도 사용할 수 있을 것 같은데 이거는

여러가지 뭐 여러분들이 처한 보안 상황에 따라서 달라질 것 같습니다.

그래서 어쨌든 적절한 걸 여러분들이 선택을 하면 될 것 같고요.

저는 지금 실습을 위해서 하는 거니까 그냥 30일 정도로 하겠습니다.

만약 여러분들은 실습을 위해서 이거를 사용하는 게 아니라 실제 이거를 Jenkins에다

세팅을 해서 사용을 하려는 거면은 만료가 없는 걸로 지정하는 게 좀 더 좋을 것 같아요.

여러분들이 알아서 해주시면 될 것 같고요.

그리고 이 WebHook을 하기 위해서는 여기 있는 이 Admin Repository

Hook이라고 되어 있는 이거에 대해서 설정을 체크를 해주면 됩니다.

이것만 해줘도 웹 훅을 사용할 수 있어요.

만약에 여러분들이 프라이빗 레포지토리를 사용하고 있다 그러면은 여기 있는 이 부분에 대해서

체크를 해주시면 됩니다.

그러면은 여러분들이 지금 기존에는 받아오던 이 레포지토리 코드가 Jenkins가 이미

프라이빗 레포지토리이기 때문에 잘 받아오고 있겠죠.

근데 그게 만약에 프라이빗 레포지토리였다고 하면은 이런 토큰을 만들어주고 거기에다

Jenkins에서 이 레포지토리, 이 GitHub으로 요청을 할 때 이 레포지토리를 조회할

수 있는 토큰을 같이 넣어서 보내줬어야 됐어요.

근데 이미 여러분들이 실습을 따라오고 있다는 거는 아마도 프라이빗 레포지토리가 아니라

퍼블릭 레포지토리라는 걸 겁니다.

그래서 어쨌든 이후에 이거를 만약에 프라이빗 레포지토리에 대해서 CI/CD를 구축을 하고

싶으면은 여기 있는 걸 사용을 하면 되겠죠?

이 권한을 체크해서 사용해야 되는 거고 나머지들도 한번 어떤 권한들이 있는지 한번

읽어보세요. 저도 거의 사용하지는 않는데 어떤 권한들이 있는지 정도는 보시는 것도 좋을

거라고 생각합니다.

자, 아무튼 여기서 Admin에 레포지토리 훅 이거에만 체크를 하고

Generate Token을 해줍니다.

자, 그러면 이렇게 이 토큰이 나와요.

근데 이 토큰 같은 경우 여기서만 지금 볼 수 있고 나중에 다시 볼 수 있는 방법이

없습니다. 그래서 이거를 여러분들이 반드시 어딘가에 적어두셔야 됩니다.

저도 따로 적어둘게요.

참고로 이게 제 계정에 대한 지금 토큰이 만들어졌는데 이건 제가 강의를 모두 촬영하고 난

다음에는 이 토큰을 만료시킬 거기 때문에 이걸로 쓸데없이 API 요청을 해보실 필요는

없습니다.

자, 이번에는 다시 이 배포 대상이 될 레포지토리로 왔고요.

여기서 이제 WebHook을 설정을 해주겠습니다.

Settings로 넘어와서 이 Settings대에 왼쪽에 Webhooks라고 있어요.

그리고 여기서 Add WebHook 버튼을 눌러서 WebHook을 추가할 수 있습니다.

자, 이제부터 WebHook을 설정을 해야 되는데 먼저 이 Payload URL이라고 되어

있는 이 부분에는 여러분들의 Jenkins IP 주소랑 그리고 뒤쪽에

고정된 경로가 있어요.

github-webhook/이라는 이 URL로 요청을 해주면 됩니다.

앞쪽에 HTTP도 같이 붙여줘야 되니까 깜빡하지 마시고요.

아, 8080 포트로 우리가 띄웠으니까 이렇게 정확히는 해줘야겠네요.

그리고 이 컨텐츠 타입을 지정하는 게 있는데 이 요청이 JSON으로

날아갈지 아니면 URL 인코드로 날아갈지 지정하는 건데 일반적으로 JSON을 많이

사용합니다.

그리고 이 밑에는 어떤 경우에 이 WebHook을 보낼 건지 지정할 수 있는데요.

지금 선택되어 있는 건 Just the push event라고 되어 있는 이

push 이벤트가 발생했을 때만 보낼 수 있게 되어 있습니다.

그리고 Send me everything은 모든 GitHub 내에 존재하는 이벤트에

대해서 webhook을 전송하겠다는 의미예요.

그리고 Let me select individual events로 들어가면 여기에는 여러

가지 GitHub 내에서 일어날 수 있는 액션들이 있어요.

이런 것들에 대해서 등록을 해 줄 수도 있습니다.

여기 보시면 이 Push가 아까 첫 번째 있었던 거랑 동일한 거죠.

그래서 여러분들이 입맛에 맞게 이 WebHook을 지정해 줄 수 있습니다.

근데 지금 우리는 이 Push 이벤트에 대해서 걸어 줄게요.

여기 있는 이 Individual Events에서도 여러 가지를 해줄 수 있는데 예를

들면 이 브랜치가 떴을 때 어떤 특정한 동작을 하도록 하던지 아니면 뭐 여기 밑에 있는 이슈가

새로 등록이 됐을 때 알림을 받을 수 있도록 한다던지 그리고 아래쪽에 이 Pull Request 관련해서 리뷰가

등록이 됐다던지 이런 것들도 전부 다 등록을 할 수 있어요.

이거는 여러분들이 어떤 것들이 있는지 한번 보시고 필요할 때 사용을 하시면 되겠습니다.

그리고 부록에다가 이 각각의 이벤트들에 대해서 이 WebHook 이벤트가 보내질 때 어떤

JSON 바디가 들어가는지도 모든 이벤트에 대해서 제가 보여드릴 건 아니지만 이 Push

이벤트가 발생했을 때 어떤 것들이 들어가는지 그리고 이 Individual Event에서도 뭐 하나

정도는 보여드리겠습니다.

어쨌든 이런 식으로 이 Push 이벤트가 발생했을 때 WebHook이

날아가도록 하는 거고요.

여기서 Active는 처음부터 체크되어 있는데 이거를 활성화, 비활성화

할 수 있을 거예요.

그래서 여기서 바로 우리는 지금 활성화를 할 거니까 Add WebHook을 눌러 주겠습니다.

그러면 이렇게 WebHook이 하나 생성이 된 겁니다.

이 상태에서는 어떤 일이 일어나냐면 디플로이 테스트라는 우리가 만들어준 GitHub

레포지토리에 Push, 즉 로컬에서 작업한 어떤 내용에 대해서 커밋하고

Push를 했을 때

이 WebHook이 실행이 됩니다.

이 WebHook은 우리가 여기다 등록해준 Jenkins 8080 포트의 GitHub

Webhook이라는 경로로 이 WebHook 요청이 발생했다는 것, WebHook 이벤트가

발생했다는 것을 보내주게 됩니다.

그것을 Jenkins가 받아서 빌드를 시작하면 되겠죠?

빌드를 하고 배포하는, 그리고 우리가 Jenkins에 들어가서 배포 명령을 직접 했던

그것을 그대로 실행하게 되는 겁니다.

그러면 이번엔 Jenkins로 가서 두 가지를 해줘야 되는데요.

우리가 GitHub에서 발급받은 토큰 등록을 먼저 해줘야 되고, 그리고 다음으로는

WebHook을 받아서 수행할 수 있도록, 파이프라인이 실행될 수 있도록 설정을

해줘야겠죠? 그 두 가지를 한번 해보겠습니다.

먼저 토큰을 등록을 해주려고 Jenkins 관리에서 아래쪽에 Credentials로 이동을

합니다.

그리고 여기 Deploy SSH 키를 등록해줬던 것도 있는데 그 아래쪽에 동일한

요령으로 Domains에 있는 Global을 눌러주세요.

그리고 다음으로는 Add Credentials를 눌러주시고 다음으로는 여기 있는 이

Kind에서는 Username with Password를 눌러주시면 됩니다.

그리고 다음으로 Scope는 따로 건드려 주실 필요는 없고요.

유저 네임에는 여러분들이 GitHub 아이디를 입력을 하면 돼요.

저 같은 경우엔 lle0을 입력을 합니다.

그리고 여기 있는 이 Password 부분에 아까 복사해 주셨던 그 GitHub 토큰을 입력을

이렇게 입력을 해줬고요.

그리고 다음으로 이 ID는 앞서 우리가 ssh-deploy-key를 등록을 했던 것처럼 이

Jenkins 내에서 이 시크릿을 뭐라고 지칭할지에 대한 거거든요.

그래서 github-token 이런 이름이면 괜찮을 것 같습니다.

설명은, 디스크립션은 따로 작성하지 않고 Create를 해주겠습니다.

Shorten URL Deploy의 설정을 바꿔야 합니다.

구성으로 돌아와서 GitHub 프로젝트 이름을 지정합니다.

이 레포지토리 URL을 등록해 주시면 됩니다.

아마 이게 등록된 단위를 기준으로 이 Jenkins가 WebHook을 여러가지 프로젝트들에

대해서 받을 수 있는데 이걸 가지고 아마 구분을 할 거예요.

그래서 이거를 등록을 해줘야지 정상적으로 이 WebHook이 빌드가 시작이

될 거고요.

그리고 아래쪽에는 여기 있는 이

github-hook-trigger-for-git-scm-polling 이것도 체크를

해주셔야 됩니다.

그러니까 빌드 트리거로 뭘 할지를 여기다 지정을 하는 건데 여러가지 트리거들이 있는데 그

중에서 GitHub에서 오는 훅을 트리거로 사용을 하겠다는 겁니다.

WebHook을 말하는 거죠.

그리고 아래쪽으로 내려서 자 이게 지금은 우리가 퍼블릭 레포지토리니까

상관이 없는데 이게

만약에 프라이빗 레포지토리였다면 여기다가 이 크리덴셜 아이디를 지정을

해줘야 됩니다.

크리덴셜 아이디로 조금 전에 등록해 준 github-token 이거를 같이 지정을 해

줘야겠죠.

이렇게 해주지 않으면 만약에 프라이빗 레포지토리일 때는 빌드가 안 될 거예요.

여기까지 하고 나머지는 아직 바꿔줄 필요는 없습니다.

저장을 해주고요.

그럼 이번에는 Push를 한번 시켜봐야겠죠.

Push를 시켜보기 위해서 여기서 README.md를 그냥 아무렇게나 한번 바꿔보겠습니다.

Commit changes를 하면 아마 Push 이벤트가 발생이 될 거예요.

이렇게 했더니 이제 Jenkins로 와서 실제로 Push 이벤트가 발생이 됐는지 새로고침을 해

보겠습니다.

보시면 지금 새로운 빌드가 시작이 되려고 하고 있죠.

잠시 기다렸더니 빌드가 실제로 시작이 됩니다.

지금 보시는 것처럼 빌드가 우리가 여기서 지금 Build라는 버튼을 눌러서 한

게 아니라

GitHub에 단순히 Push가 되기만 했는데도 이렇게 빌드가 시작이 되고 있는 걸 볼 수

있습니다.

이게 바로 GitHub WebHook을 활용해서 배포를 한 번 더 자동화한

거죠.

자 그런데 배포가 잘 될 줄 알았는데 분명 시작은 잘 됐습니다.

근데 디플로이 과정에서 실패를 했죠.

자 이 부분은 제가 시작하면서 말씀을 드렸던 기존 스크립트가 가지고 있던

한 가지 문제점이 있습니다.

그게 뭔지 한번 로그를 보면서 확인을 해볼게요.

가장 마지막 부분으로 가보시면 지금 중간에 이 애플리케이션을 처음에 시작하다가 뭔가

문제가 발생한 것 같아요.

문제가 발생했는데 이게 무슨 문제인지는 이 로그 상에서는 사실 잘 모르겠습니다.

분명히 여기서 20초 동안 기다리고 그리고 이 애플리케이션을 실행시키는

명령어까지는 실행이 된 것 같습니다.

근데 왠지 모르겠지만 이 루트의 app.log는 네, 파일이 없다고 나오네요.

왜 없다고 나오는지는 한번 서버에 들어가서 보면 좋을 것 같은데요.

한번 서버에 SSH로 접속을 해서 뭐가 문제인지 확인을 한번 해보겠습니다.

네, 이렇게 애플리케이션 서버로 들어왔는데요.

애플리케이션 서버에서 아마 이 app.log에 뭔가

남아있을 것 같습니다.

그래서 app.log를 이렇게 찍어보면 여기에 힌트가 잘 남아있어요.

지금 보시는 것처럼 이 8080 포트가 다른 애플리케이션에 의해서 이미 사용 중인 것

같다고 나옵니다.

Listening 된 상태라고 나오죠.

그래서 이미 이 8080 포트를 사용하고 있는데 이 8080 포트를 사용하고 있는 게 이미

있습니다.

이렇게 입력을 해 보시면 우리가 기존에 띄워놨던 이 Spring Boot

애플리케이션이 이렇게 떠 있는 거예요.

왜 이런 결과가 나왔을까요?

아마 추측하셨겠지만 그 이유는 바로 우리가 만들어준 이 배포 스크립트에는 애플리케이션을

새로 띄우는 것만 있지

기존에 떠있던 애플리케이션을 종료하는 과정은 없습니다.

따라서 우리가 새로운 코드로 새로운 애플리케이션을 빌드해서 배포를 해야 되는데 기존

애플리케이션이 떠 있기 때문에 새로운 애플리케이션이 뜰 수가 없는 거죠.

그래서 우리가 이 스크립트에다가 추가해줘야 되는 건 기존 애플리케이션을

종료하는 코드를

넣어줘야 됩니다.

기존 애플리케이션은 먼저 종료하고 새로운 버전의 애플리케이션을 띄우는 거죠.

그럼 먼저 우리가 Jenkins에 등록했던 Pipeline 스크립트로 가겠습니다.

Pipeline 스크립트를 보면 마지막 부분이 현재 이렇게 되어 있습니다.

```groovy
ssh -o StrictHostKeyChecking=no root@서버IP \
"java -jar /root/shortenurlservice-0.0.1-SNAPSHOT.jar \
> /root/app.log 2>&1 &"
```

즉, 그냥 새로운 애플리케이션을 실행만 하고 있습니다.

기존 프로세스를 종료하는 명령은 전혀 없는 상태죠.

그래서 실행 전에 기존 Spring Boot 프로세스를 먼저 종료하도록 수정하겠습니다.

Linux에서는 `pkill` 명령을 사용할 수 있습니다.

`pkill -f` 옵션은 실행 중인 프로세스의 명령어(Command Line)에 특정 문자열이

포함되어 있으면 종료시키는 명령입니다.

따라서 우리가 실행하는 jar 파일 이름을 기준으로 종료를 시키면 됩니다.

예를 들어 아래처럼 작성할 수 있습니다.

```bash
pkill -f shortenurlservice-0.0.1-SNAPSHOT.jar
```

그러면 기존에 실행 중이던 Spring Boot 프로세스가 종료됩니다.

하지만 여기에도 한 가지 문제가 있습니다.

처음 배포하는 경우에는 아직 실행 중인 프로세스가 없죠.

그러면 `pkill`은 종료할 프로세스를 찾지 못해서 오류 코드를 반환합니다.

그런데 Jenkins는 기본적으로 중간 명령 하나라도 실패하면

Pipeline 전체를 실패로 처리합니다.

그래서 종료할 프로세스가 없다는 이유만으로

배포가 실패하면 안 되겠죠.

그래서 뒤에 `|| true`를 붙여줍니다.

```bash
pkill -f shortenurlservice-0.0.1-SNAPSHOT.jar || true
```

이렇게 하면

프로세스가 있으면 종료하고,

없으면 그냥 성공한 것처럼 다음 명령을 계속 실행합니다.

그리고 프로세스가 종료되는 데에는 약간의 시간이 필요하기 때문에

잠깐 기다려 주는 것이 좋습니다.

보통 3~5초 정도면 충분합니다.

```bash
sleep 5
```

그리고 나서 새로운 애플리케이션을 실행합니다.

결국 스크립트는 아래와 같은 형태가 됩니다.

```bash
pkill -f shortenurlservice-0.0.1-SNAPSHOT.jar || true

sleep 5

java -jar /root/shortenurlservice-0.0.1-SNAPSHOT.jar \
> /root/app.log 2>&1 &
```

그러면 이제 Jenkins Pipeline에서도

SSH로 접속한 뒤

위 명령들을 순서대로 실행하도록 수정하면 됩니다.

Pipeline에서는 다음과 같이 작성할 수 있습니다.

```groovy
ssh -o StrictHostKeyChecking=no root@서버IP "
pkill -f shortenurlservice-0.0.1-SNAPSHOT.jar || true

sleep 5

java -jar /root/shortenurlservice-0.0.1-SNAPSHOT.jar \
> /root/app.log 2>&1 &
"
```

여기까지 하면

기존 애플리케이션을 종료하고,

잠시 기다렸다가,

새로운 버전을 실행하는 기본적인 배포가 완성됩니다.

다시 저장하고

GitHub에 Commit 후 Push를 해보겠습니다.

Push가 완료되면

GitHub WebHook이 Jenkins로 요청을 보내고,

Jenkins가 자동으로 Pipeline을 실행합니다.

빌드가 끝난 뒤 서버에 접속해서

이번에는 정상적으로 실행됐는지 확인해보겠습니다.

Jenkins 로그를 보면 이번에는 오류 없이 모든 단계가 완료되었습니다.

그러면 서버에서도 확인해보겠습니다.

```bash
ps -ef | grep java
```

실행해 보면 Spring Boot 애플리케이션이 정상적으로 실행되고 있는 것을

확인할 수 있습니다.

그리고 브라우저에서 서버에 접속해 보면

방금 수정한 코드가 정상적으로 반영된 것을 확인할 수 있습니다.

여기까지 하면

GitHub에 Push만 해도

Jenkins가 자동으로 빌드하고,

애플리케이션을 서버에 복사한 뒤,

기존 프로세스를 종료하고,

새로운 버전을 실행하는

기본적인 CI/CD 자동 배포 환경이 완성된 것입니다.

그렇게 한번 코드를 바꿔볼게요

기존 애플리케이션을 종료시키는 코드를 넣어야 되는데 스크립트에 배포될

지금 이게
실행이 되고 있는 프로세스 인지를 먼저 확인을 하고 만약에 실행이
되고 있으면 그 경우에는

그렇게 프로세스를 키라는 것까지 진행을 하는 겁니다

아마 kill 명령어가 제가 알기로는 단독으로 실행이 됐을 때 프로세스
만약에

없으면은 이게 배포에 실패를 할 거에요

그래서 이런 식으로 코드를

작성해 준 것 같습니다

그렇게 하고서 한번 배포를 시켜 볼게요

배포는 당연히 이번에도 Deploy 파일을 수정해서 배포해보겠습니다.

이번에는 정상적으로 배포가 됐습니다.

실제로 배포가 잘 됐는지는 이 애플리케이션을 접속해보면 되겠죠?

애플리케이션을 접속해보기 위해서 여기로 접속을

하면 될 겁니다.

자 여기까지 하고서 마칠 건 아니고 이제 이 다음 영상부터 이야기할
주제가 바로

무중단 배포에 대한 내용입니다.

우리가 하고 있는 건 배포 자동화긴 하지만 중간에 배포를 하고 있는 거죠.

물론 이거를 실제로 중단이 얼마나 있었는지는 스트레스 테스트 툴 같은 걸로 계속 이거를

매초마다 요청을 날린다든지 이런 걸로 한번 확인해볼 수가 있겠죠?

근데 지금 이렇게 간단하게 개발자 도구만으로도 확인해볼 수가 있는 겁니다.

그래서 다음 시간부터는 여기 있는 이런 애플리케이션 배포 사이에 발생하는 중단을 없애는

방법에 대해서 이야기를 드리겠습니다.

마지막으로 오늘 바꾼 이 스크립트 역시 여기에다가 업로드를 해놨습니다.

1, 2 파이프라인 스크립트예요.

달라진 부분은 여기밖에 없어서 여러분들이 직접 하는 것도 크게 문제는 없을 것 같습니다.

여기까지 WebHook을 활용해서 코드가 푸시됐을 때 자동으로 배포가 시작되도록

그리고 기존 실행하고 있던 애플리케이션이 종료되고 새로운 애플리케이션이 실행되도록

해봤습니다.

다음 시간부터는 무중단 배포에 대해 이야기를 하겠습니다.